[mysqld]
# MySQL/MariaDB runs on 5054 (not 3306) to avoid colliding with Webuzo.
# Host networking: bind to loopback only so the DB is NOT exposed to the network;
# WordPress (same host namespace) reaches it at 127.0.0.1:5054.
port = 5054
bind-address = 127.0.0.1
skip-name-resolve
